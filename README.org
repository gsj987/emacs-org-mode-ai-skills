#+TITLE: org-ai-skills
#+STARTUP: overview

* Release Notice

This repository was built via vibe coding and I have not fully reviewed all code paths.
I cannot guarantee that there is no harmful behavior in this codebase.

This project is a complete personal workflow.
It is shared to communicate with people interested in this topic, and discussion is welcome in GitHub Issues.

* Project Goal

org-ai-skills is an experimental Emacs system for defining, executing,
and evolving AI Skills using Org Mode and gptel.

The system treats Skills as structured cognitive procedures
that can be planned, executed, reviewed, and evolved safely.

This project aims to:

- Define a minimal Skill specification model
- Separate Skill spec from Run state
- Provide Planner + Run Ticket mechanism
- Enable controlled Skill self-evolution
- Use dev-agent-work style AI-driven development workflow

This is an experimental cognitive runtime for Emacs.

* Architecture Overview

Core concepts:
- Skill Spec (Org-defined, read-only)
- Run Ticket (execution instance)
- Proposal (change suggestion)
- Governance Gate (approval before commit)

System layers:

1. Skill Registry
2. Planner
3. Execution via gptel
4. Run Tracking
5. Proposal & Governance

* Directory Structure

- elisp/         → Emacs Lisp source code
- skills/        → Skill spec definitions
- prd/           → Product requirement documents
- tests/         → ERT test cases

* Development Workflow

This project follows an AI-assisted workflow inspired by:
https://github.com/farra/dev-agent-work

Each feature iteration includes:

1. Write or update PRD in prd/ following the =prd/000-template.org= 
2. Implement feature in elisp/
3. Write ERT tests
4. Run tests locally
5. Update README if behavior changes
6. Append entry to the prd org file

No direct feature implementation without PRD.

* Testing

We use ERT for testing.

To run tests:
=M-x ert RET t RET=

All new features must include tests.

** BDD + E2E (Phase 1)

Phase 1 adds Org-based BDD scenarios under =tests/bdd/= and a live E2E runner that executes real gptel calls in isolated runtime state.

- Scenario file examples:
  - =tests/bdd/000-template.org=
  - =tests/bdd/001-twitter-shorten-subtree.org=
  - =tests/bdd/002-generate-structured-notes-subtree.org=
  - =tests/bdd/003-daily-financial-news-report-subtree.org=
  - =tests/bdd/004-article-outline-from-source-subtree.org=
  - =tests/bdd/005-article-compose-from-outline-subtree.org=
  - =tests/bdd/006-article-repair-subtree.org=
  - =tests/bdd/007-article-polish-editorial-subtree.org=
- Scenario file keeps fixture data inline via sections:
  - =* Fixture Input=
- Scenario and fixture headings can include Org property drawers for richer cases.
- Optional fallback remains supported:
  - Given =fixture file "..."=

Simple live E2E command:

#+begin_example
emacs -Q --batch \
  -l tests/e2e-init.el \
  -l tests/e2e-tests.el \
  -f ert-run-tests-batch-and-exit
#+end_example

Tool-call coverage (must-pass live check):
#+begin_example
emacs -Q --batch \
  -l tests/e2e-init.el \
  -l tests/e2e-tests.el \
  --eval '(ert-run-tests-batch-and-exit "org-ai-skills-e2e-live-tool-call-financial-report")'
#+end_example

Tool-call provider order defaults to:
- =atlas-cloud/fp8,alibaba=

Override if needed:
#+begin_example
export ORG_AI_SKILLS_OPENROUTER_TOOL_PROVIDER_ORDER="provider1,provider2"
#+end_example

Put your gptel backend/api-key/model init in:
- =tests/e2e-init.el=

Default E2E model profile:
- =qwen/qwen3-8b=

* Usage (Initial Version)

Add to init.el:

=(add-to-list 'load-path "/path/to/org-ai-skills/elisp")=
=(require 'org-ai-skills)=

Enable mode:

=M-x org-ai-skills-mode=

* Usage (Skill Spec v1)

** org-skill Spec (v1)

The first =org-skill= spec is file-based and validated with explicit rules.

Scope in v1:
- Discover only =*.org= files under =skills/=
- Parse one skill per file (top-level heading)
- Validate required fields and semantic tag values
- Return deterministic structured data for gptel integration

Required structure:
- Heading: =* Skill: <Skill Name>=
- Property drawer key: =:SKILL_ID: <id>=
- Required semantic tags:
  - =#+EFFECT:= one of =pure/local/external/irreversible=
  - =#+INVOCATION:= one of =auto/suggest/manual=
  - =#+CONTEXT:= one of =buffer/project/global=
  - =#+DETERMINISM:= one of =deterministic/heuristic=
- Required section: =** Description=

Minimal valid example:

#+begin_example
,* Skill: Generate Structured Notes
:PROPERTIES:
:SKILL_ID: gen-notes
:END:

#+EFFECT: pure
#+INVOCATION: suggest
#+CONTEXT: project
#+DETERMINISM: deterministic

,** Description
Transform selected Org content into a concise, structured outline.
#+end_example

Parse output contract (=org-ai-skills-parse-skill-file=):
- Returns plist keys:
  - =:file=
  - =:title=
  - =:skill-id=
  - =:description=
  - =:outputs= (list)
  - =:contracts= (list)
  - =:requirements= (list)
  - =:function-calls= (list of plists)
  - =:function-definitions= (list of emacs-lisp source block strings)
  - =:raw-sections= (plist)
  - =:tags= with nested plist:
    - =:effect=
    - =:invocation=
    - =:context=
    - =:determinism=

Failure behavior:
- Signals =org-ai-skills-parse-error= for:
  - Missing required fields/section
  - Invalid semantic tag values
  - Unreadable skill files
- Parser is read-only and does not modify files under =skills/=

gptel mapping contract (=org-ai-skills-build-gptel-payload=):
- Returns plist keys:
  - =:skill-id=
  - =:skill-title=
  - =:goal=
  - =:description=
  - =:tags=
  - =:prompt=

Load and inspect the first skill spec:

#+begin_example
(add-to-list 'load-path "/path/to/org-ai-skills/elisp")
(require 'org-ai-skills)

;; Discover skill files under skills/
(org-ai-skills-discover-skill-files)

;; Parse and validate one skill
(setq skill
      (org-ai-skills-parse-skill-file
       "/path/to/org-ai-skills/skills/001-generate-structured-notes.org"))
#+end_example

Build a structured payload for gptel:

#+begin_example
(org-ai-skills-build-gptel-payload
 skill
 "Draft a product specification for org-ai-skills plugin")
#+end_example

Example skill set for composition testing:
- =skills/001-generate-structured-notes.org=
  - Expands and structures subtree content with richer detail.
- =skills/002-simplify-for-twitter-post.org=
  - Compresses subtree content into concise, tweet-ready wording
    (including <= 280 character tweet sentence constraint in contracts).
- =skills/003-daily-financial-news-report.org=
  - Uses Search1API-backed raw data function call; LLM formats final Org report.
- =skills/004-article-outline-from-source.org=
  - Builds article outline subtree from source context with =:PURPOSE:= guidance.
- =skills/005-article-compose-from-outline.org=
  - Expands approved outline into draft sections.
- =skills/006-article-repair-subtree.org=
  - Regenerates selected weak subtree while preserving surrounding scope.
- =skills/007-article-polish-editorial.org=
  - Runs final editorial consistency pass for wording/tone.

Load gptel from straight checkout path if needed:

#+begin_example
(org-ai-skills-require-gptel "~/.emacs.d/straight/repos/gptel/")
#+end_example

* Usage (Org Subtree Rewrite via gptel)

This feature rewrites an Org headline subtree with skill-guided prompting.

Interactive flow:
- Open any Org buffer.
- Place point in a target headline subtree.
- Run:
  - =M-x org-ai-skills-org-rewrite-subtree=
  - Or strict mode: =M-x org-ai-skills-org-rewrite-subtree-strict=
- Choose rewrite scope from minibuffer preview list (org-refile-like):
  - = [buffer] /path/to/file.org= (whole buffer)
  - = [current] Top/Child/Leaf=
  - = [up:1] Top/Child=
  - = [up:2] Top=
- Choose a skill from =skills/=.
- Optional: add a custom rewrite instruction.
- Strict mode guarantees:
  - headline lines stay unchanged at all levels (same stars + same text), otherwise apply is rejected.
  - model output property drawers are stripped before candidate persistence/apply.
- One command run generates one candidate output and stores it.
- Interactive runs auto-open a 2-column workspace:
  - left: =*org-ai-skills-control*= (run status + operation keys)
  - right: source Org buffer (target region overlay)
- Runs auto-apply the newly generated candidate by default when target markers are valid.
- Apply target resolution uses stable slot ID first (then marker fallback) to reduce wrong-subtree replacement risk during async runs.
- Disable auto-apply if needed:
  - =(setq org-ai-skills-auto-apply-generated-candidate nil)=
  - Then select/apply manually from candidate list.
- Disable auto-open workspace if needed:
  - =(setq org-ai-skills-ui-auto-open nil)=
- Tune control-panel width (columns):
  - =(setq org-ai-skills-ui-control-window-width 34)=
  - Runtime also caps control width at half of current frame.

Control workspace keys (in =*org-ai-skills-control*=):
- =s=: stop current run (enabled only while status is =running=)
- =g=: rerun with same context (dispatches a new run; unavailable while running)
- =t=: adjust planner task / rewrite instruction, then rerun (unavailable while running)
- =v=: view execution DAG for current planner run
- =c=: open minibuffer candidate selector
- =a=: apply currently selected candidate (unavailable while running)
- =d=: mark selected candidate as discarded (unavailable while running)
- =x=: manually extract reusable patterns from selected/latest candidate into proposal artifact (unavailable while running)
- =p=: select proposal from current slot (minibuffer)
- =v=: preview selected proposal details in =*org-ai-skills-proposal*=
- =y=: approve selected proposal (unavailable while running; only =proposed= proposals)
- =n=: reject selected proposal (unavailable while running; supports =proposed= or =approved= proposals)
- =m=: apply selected proposal (artifact-only apply; requires =approved=)
- =M=: apply selected proposal to target skill file (confirmed; requires =approved=; unavailable while running)
- =q=: close workspace (run continues in background; use =s= to stop)
- =r=: refresh control buffer

Control panel details:
- On first workspace activation, focus moves to control panel for key-driven operation.
- Key rows are highlighted when available and dimmed when unavailable.
- Failed runs show explicit =Error:= detail in panel (for example provider tool
  policy/runtime errors), not only status/progress markers.
- Rerun/adjust/apply actions execute in recorded source Org buffer only; if
  source context is missing, panel now reports explicit org-context error.
- Candidate history list is rendered directly in control panel for long review flows.
  - Compact format: index + status + short preview (IDs/timestamps hidden in panel).

Candidate apply command:
- =M-x org-ai-skills-org-apply-slot-candidate=
- Select target scope, then pick one saved candidate from minibuffer.

Core APIs:
- Resolve target subtree:
  - =(org-ai-skills-org-resolve-subtree 'current)=
  - =(org-ai-skills-org-resolve-subtree 'upper-level 1)=
- Build rewrite request payload:
  - =(org-ai-skills-build-gptel-rewrite-request skill subtree "Rewrite concisely")=
- Build normalized skill context bundle:
  - =(org-ai-skills-build-skill-context skill subtree)=
- Dispatch to gptel:
  - =(org-ai-skills-gptel-dispatch-rewrite request callback)=
- Install Embark action on Org headings:
  - =(org-ai-skills-embark-install-action)=

Failure behavior:
- Signals =org-ai-skills-org-context-error= for invalid Org context/ranges.
- Signals =org-ai-skills-gptel-error= if gptel is unavailable.
- Signals =org-ai-skills-function-call-error= for invalid/missing skill function calls.

Output handling:
- Rewrite prompt enforces artifact-only output (no explanation/progress text).
- Response sanitizer strips leading chatter/code fences and normalizes heading levels
  so replacement root level always matches selected target subtree level.
- Rewrite request includes =:skill-context= with contracts/requirements/function-calls
  and source subtree metadata (including inherited =:PURPOSE:= and
  =:SOURCE_FILE_PATH:= when present).
- Purpose/source-path metadata resolution supports both:
  - headline property drawer keys: =:PURPOSE:=, =:SOURCE_FILE_PATH:=
  - Org file keywords fallback: =#+PURPOSE:=, =#+SOURCE_FILE_PATH:=
  - =:SOURCE_FILE_PATH:= is explicit-only: runtime does not infer it from current Org file path.
- Each run persists one candidate version under:
  - =~/.emacs.d/skills/versions/=
- Pattern extraction is manual (not auto-triggered per run):
  - Use =x= in control panel to create one proposal artifact from current candidate state.
- Each extracted proposal is bound to one target skill inferred from previous run context:
  - rewrite run: current rewrite skill
  - planner run: last completed step skill in planner run-state
  - extraction fails if target skill cannot be resolved
- Proposal artifacts are persisted under:
  - =~/.emacs.d/skills/proposals/=
- Proposal audit log is appended at:
  - =~/.emacs.d/skills/proposals/audit-log.jsonl=
- Candidate store read/write uses UTF-8 by default.
- Runtime safety gate:
  - proposal apply refuses runtime mutation under =skills/= (artifact-only apply path).
- Confirmed skill-file apply workflow:
  - Approve proposal first (=y=), then run =M=.
  - System asks confirmation and appends an extracted-pattern block to =:target-skill-file=.
  - Proposal status transitions to =applied= with application mode =skill-file-append=.
- If headline =:ID:= is missing, runtime auto-generates and writes it back before candidate persistence.
- Overlay behavior:
  - running: highlight target region as processing
  - ready: mark pending replacement candidate
  - cleared on stop/apply/workspace close and immediately after candidate switch
- Function calls are skill-scoped runtime state:
  - Declared calls are available only while a skill is actively applied.
  - Excluding or finishing a skill unloads its function calls from request context.
  - Function implementations are loaded from skill-local Org code blocks
    (section =** Function Definitions=, =#+begin_src emacs-lisp ...=).

Function call lifecycle APIs:
- =(org-ai-skills-apply-skill-function-calls skill)=
- =(org-ai-skills-exclude-skill-function-calls skill-or-id)=
- =(org-ai-skills-active-skill-function-calls skill-or-id)=
- =(org-ai-skills-clear-active-skill-functions)=

gptel tool registration behavior:
- Active skill =Function Calls= are converted to request-scoped gptel tools.
- Core read tools are exposed by default on all requests:
  - =org-ai-skills-read-buffer= (buffer/range reads)
  - =org-ai-skills-read-file= (source file reads)
- Optional phase-1 core provider tools can be enabled for complex task execution:
  - =org-ai-skills-core-provider-read-file=
  - =org-ai-skills-core-provider-list-files=
  - =org-ai-skills-core-provider-search-files=
  - =org-ai-skills-core-provider-shell-exec=
  - =org-ai-skills-core-provider-python-exec=
  - =org-ai-skills-core-provider-org-babel-exec=
- Planner requests do not expose tools, to keep planner JSON schema output deterministic.
- Toggle core read tools:
  - =org-ai-skills-enable-core-read-tools= (default =t=)
- Toggle phase-1 core provider tools:
  - =org-ai-skills-enable-core-provider-tools= (default =t=)
- Phase-1 provider allowlists:
  - =org-ai-skills-core-provider-allowed-commands=
  - =org-ai-skills-core-provider-allowed-paths=
- Provider directory resolution:
  - If a tool call omits =directory=, runtime uses request =:working-directory= when available.
  - Working directory is inferred from subtree context (priority: path in =:PURPOSE:= text, then =:SOURCE_FILE_PATH:=).
- Provider error propagation:
  - If provider tool-result returns =:ok nil= with =:error-kind= (for example =path-not-allowed=), rewrite/planner execution fails fast and does not auto-apply generated content.
- Read cap for official file reads:
  - =org-ai-skills-core-read-max-chars= (default =20000=)
- Per-argument hints can be declared in =Function Calls= (for example =query:=,
  =limit:=, =language_hint:=, =date:=) and are propagated to gptel tool arg
  descriptions.
- Planner still decides skill selection and step execution order.
- Runtime passes registered tools via =gptel-tools= and =gptel-use-tools= during
  each =gptel-request= dispatch.

Role-scoped model and system prompt routing:
- Planner requests use role =planner=.
- Rewrite and step-execution requests use role =execution=.
- Model overrides:
  - =org-ai-skills-model-planner=
  - =org-ai-skills-model-execution=
  - Set either to =nil= to use gptel default model selection.
- Planner generation stability controls:
  - =org-ai-skills-planner-temperature= (default =0.0=)
  - =org-ai-skills-planner-max-tokens= (default =2048=, or =nil= for backend default)
  - Planner requests also use gptel structured output schema (= :schema =)
    to enforce JSON shape at provider/backend level.
- System prompt overrides:
  - =org-ai-skills-system-prompt-planner=
  - =org-ai-skills-system-prompt-execution=
  - If set to empty string, runtime falls back to built-in default prompt for that role.
- Dispatch attaches role-specific =:system= prompt to each =gptel-request= call.
- Model routing is applied via dynamic =gptel-model= binding per request role.

Search1API credential lookup:
- API key is read from auth-source:
  - =(auth-source-pick-first-password :host "search1api")=
- Example =~/.authinfo= entry:
#+begin_example
machine search1api login apikey password YOUR_SEARCH1API_KEY
#+end_example

For the financial news example, Search1API functions are defined inside:
- =skills/003-daily-financial-news-report.org=

Debug observability:
- Toggle debug capture:
  - =M-x org-ai-skills-debug-toggle=
- Show last debug capture:
  - =M-x org-ai-skills-debug-show-last=
- Show filtered debug capture (uses configured filters):
  - =M-x org-ai-skills-debug-show-filtered=
- Set debug filters programmatically:
  - =(setq org-ai-skills-debug-filter-level 'warn)=
  - =(setq org-ai-skills-debug-filter-step "s2")=
- Variables:
  - =org-ai-skills-debug-enabled=
  - =org-ai-skills-debug-property-retention=
  - =org-ai-skills-debug-buffer-name=
  - =org-ai-skills-debug-filter-level= (=nil/debug/info/warn/error=)
  - =org-ai-skills-debug-filter-step= (step id or stage id string, or =nil=)
- Debug entry includes timestamp, request role, effective model, system prompt
  fingerprint, log level, stage id, buffer/file, target headline/path, final
  prompt, and full request plist passed to =gptel-request=.
- Additional debug events:
  - =gptel-request-data= includes request payload data captured from gptel callback info.
  - =tool-call= and =tool-result= events are logged when gptel emits tool-calling callbacks.
  - =property-retention= logs subtree property collect/restore diagnostics:
    matching strategy per heading (=path=, =exact-level-title=, =title=,
    =level-order=, =fuzzy-level-title=, or =unmatched=) and restored keys.

Run-state timing observability:
- Planner and execution flows append timing events to =run-state :events=.
- Current stage ids:
  - =planning.request=
  - =planning.parse=
  - =execution.step=
- Each event records at least:
  - =:stage-id=, =:start-ms=, =:end-ms=, =:duration-ms=, =:status=
- Token/cost fields on stage events:
  - =:usage= (normalized =:input-tokens=, =:output-tokens=, =:total-tokens=)
  - =:estimated-cost-usd=
- Usage normalization accepts common payload variants:
  - keyword/symbol keys (=input_tokens=, =prompt_tokens=, =usage_metadata=, etc.)
  - nested provider payloads (e.g., =response.usage=)
  - ollama-style counts (=prompt_eval_count= / =eval_count=)
- Run-level aggregate usage totals:
  - =run-state :metrics :usage-totals=
  - keys: =:input-tokens=, =:output-tokens=, =:total-tokens=, =:estimated-cost-usd=
- Deterministic test clock:
  - =org-ai-skills-observability-now-function= (defaults to =current-time=)
- Optional local cost estimation when provider omits cost:
  - =org-ai-skills-observability-cost-per-1k-input-tokens=
  - =org-ai-skills-observability-cost-per-1k-output-tokens=

Execution DAG observability:
- Build DAG model from run-state:
  - =(org-ai-skills-build-execution-dag run-state)=
  - =(org-ai-skills-build-execution-dag run-state nil t)= (event-instance mode)
- Render DAG text:
  - =(org-ai-skills-render-execution-dag-text dag)=
- View DAG interactively:
  - =M-x org-ai-skills-ui-show-dag=
  - In control panel: press =v=
- DAG nodes include:
  - =:id=, =:status=, =:dependencies=, =:skills=, =:metrics=
  - Interactive view auto-switches to event-instance DAG when run-state has timing events,
    so planner/replan calls appear as separate nodes (e.g., =planning.request#2=,
    =execution.step:step-2#1=).

Embark integration:
- Requires Embark installed.
- Registers action key =R= on target type =org-heading=.
- Trigger =R= from Embark on an Org heading target to run rewrite flow.

* Usage (Org Src Block Execution and Auto Debug)

This flow targets executable src blocks in any Org file (not limited to =skills/= files).

Interactive execution:
- Open any Org buffer and place point inside a =#+begin_src ... #+end_src= block.
- Run:
  - =M-x org-ai-skills-org-execute-src-block-at-point=
- Optional prompt expectation can be entered to annotate/evaluate run intent.

Core APIs:
- Resolve src block at point:
  - =(org-ai-skills-org-src-block-at-point)=
- Discover all src blocks in current Org buffer:
  - =(org-ai-skills-org-collect-src-blocks)=
- Execute one src block:
  - =(org-ai-skills-org-execute-src-block block prompt evaluator attempt append-metadata)=
- Run bounded execute/evaluate/repair loop:
  - =(org-ai-skills-org-run-src-block-auto-debug prompt repair-fn block evaluator options)=
  - =repair-fn= input: plist with prompt, attempt, language, current body, last result, and history.
  - =repair-fn= output: non-empty replacement block body string.
  - =evaluator= input: =(result prompt)=, output plist =(:ok t/nil :reason "...")=.

Supported executors (default):
- =sh=, =bash=, =shell=
- =emacs-lisp=, =elisp=

Execution/debug controls:
- =org-ai-skills-org-code-block-max-retries= (default 2)
- =org-ai-skills-org-code-block-executors= (language -> executor function map)
- =org-ai-skills-org-code-block-append-metadata= (default =t=)

Behavior notes:
- Each run returns structured output: language, stdout, stderr, exit code, duration, attempt, and evaluation status.
- Auto-debug loop stops early on success and is always bounded by retry limit.
- With =:apply-fixes t=, only the current src block body is mutated between attempts.
- When metadata append is enabled, run records are appended below the block as Org comments for traceability.

* Usage (Autonomous Planner + Lazy Skill Loading)

Planner mode adds automatic skill candidate selection, multi-step execution, and optional replanning.

Article workflow recommendation:
- Use =M-x org-ai-skills-plan-run= with stage-specific task prompts:
  - outline from source
  - compose from edited outline
  - repair selected subtree
  - editorial polish
- Choose = [buffer] ...= scope in picker when article flow should run on full file.
- Keep per-headline intent in =:PURPOSE:=.
- Use =:SOURCE_FILE_PATH:= on article root/subtree when source is external
  (supports Org/Markdown/text files).
- Compose safety defaults (=article-compose-from-outline=):
  - Headline lines are treated as immutable (outline lock).
  - Property drawers in model output are stripped before apply.
  - Prompt context is compacted to outline summary to reduce prompt bloat.
- Subtree apply ensures resulting headings have =:ID:= for rerun targeting.

Core model:
- Planner phase uses metadata-only skill index:
  - =org-ai-skills-load-skill-metadata=
- Execution phase lazy-loads full skill context only for selected step skills:
  - =org-ai-skills-load-skill-by-id=
- Planner command:
  - =M-x org-ai-skills-plan-run=
  - =M-x org-ai-skills-plan-repeat-task= (reuse previous planner task)
  - =M-x org-ai-skills-plan-run-preset= (select preset task intent)

Planner APIs:
- Build planner request:
  - =(org-ai-skills-build-planner-request task metadata run-state)=
- Parse planner response:
  - =(org-ai-skills-parse-planner-response response-text metadata)=
- Run task with planner orchestration:
  - =(org-ai-skills-run-task-with-planner task subtree)=

Balancing controls:
- =org-ai-skills-planner-max-steps= (default 6)
- =org-ai-skills-planner-max-replans= (default 2)
- =org-ai-skills-planner-auto-replan= (default t)
- =org-ai-skills-planner-max-skills-per-step= (default 2)
- =org-ai-skills-planner-batching-mode= (=quality-first=, =balanced=, =speed-first=)
- =org-ai-skills-planner-overflow-strategy= (=reject= or =split=)

Behavior notes:
- If planner returns too many skills in one step:
  - =reject=: signal planner validation error.
  - =split=: auto-split one oversized step into bounded step parts.
- If initial planner response returns empty =plan= but includes candidates:
  - Runtime synthesizes a one-step fallback plan from the top candidate.
  - If both =plan= and =candidates= are empty, runtime synthesizes fallback
    from metadata (preferred: suggest+pure skill, else first available skill).
- Planner apply behavior:
  - Interactive planner commands (=org-ai-skills-plan-run=,
    =...-repeat-task=, =...-preset=, and fixed-task commands from
    =org-ai-skills-define-plan-run-preset-command=) auto-apply the newly
    generated candidate by default.
  - Set =(setq org-ai-skills-auto-apply-generated-candidate nil)= to require
    manual candidate selection/apply.
  - For =[buffer]= scope apply, existing file-level front matter
    (for example =#+TITLE:=, =#+FILETAGS:=, =#+PURPOSE:=, =#+SOURCE_FILE_PATH:=)
    is preserved; generated content replaces the body section.
  - For =[buffer]= scope apply, explicit heading-level =:PURPOSE:= and
    =:SOURCE_FILE_PATH:= are also restored onto matching regenerated headings
    when model output omits drawers.
  - For subtree scope apply, explicit =:PURPOSE:= and =:SOURCE_FILE_PATH:=
    are preserved on matching headings when regenerated content omits them.
  - Apply flow strips malformed indented pseudo property drawers from model
    output (for example `     :PROPERTIES:`), preventing duplicate drawers.
  - Rewrite constraints are inferred from the final executed step skill only.
    Example: if compose runs first but final step is editorial polish, compose
    headline-lock constraints are not force-applied to the final artifact.
- Debug logging includes planner events, step composition reason, and per-step skill ids.
- Planner task reuse:
  - Last task is cached in =org-ai-skills--last-planner-task=.
  - Repeat command reruns planner on selected subtree with cached task.
  - If no prior task exists, repeat command signals a planner error.
- Embark keys on =org-heading=:
  - =R= manual single-skill rewrite
  - =P= planner run (prompt task)
  - =p= planner rerun with last task

Preset intents for common tasks:
#+begin_example
(setq org-ai-skills-planner-task-presets
      '(("meeting-notes" . "Rewrite into concise meeting notes with decisions and action items")
        ("rfc-brief" . "Rewrite into RFC format: Problem, Proposal, Risks, Open Questions")
        ("zh-polish" . "Polish wording and clarity; keep Chinese language and Org structure")))
#+end_example

Generate a keybind-friendly fixed-task command:
#+begin_example
(org-ai-skills-define-plan-run-preset-command
 org-ai-skills-plan-rfc-brief
 "Rewrite into RFC format: Problem, Proposal, Risks, Open Questions")

;; Example keybind
(global-set-key (kbd "C-c a r") #'org-ai-skills-plan-rfc-brief)
#+end_example

* Contribution

All changes must:

- Include PRD update
- Pass tests
- Update worklog
- Be reviewed through governance workflow
