#+TITLE: BDD 011 - Execution stall fails fast

* Feature
Execution steps should fail explicitly instead of hanging when callbacks stall.

* Scenario: Execution should not stay running indefinitely after tool-call stalls
Given target heading "Execution Stall Fail Fast"
And fixture source is "inline section: Fixture Input"
And planner task "生成今天的金融日报"
And model "minimax/minimax-m2.5"
And provider mode "live"
When user runs command "org-ai-skills-plan-run"
Then run status should be one of "applied,success,done,failed"
And dag info should contain "Execution DAG"

* Fixture Input
** Execution Stall Fail Fast
 :PROPERTIES:
 :PURPOSE: Validate execution-step stall fail-fast behavior.
 :END:
This subtree is used to validate execution-step timeout/fail-fast behavior.
